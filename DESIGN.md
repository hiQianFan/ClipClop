---
name: ClipClop
description: 克制、精确、以内容为中心的桌面剪贴板工具
colors:
  dark-shell: "#17181A"
  dark-raised: "#1E2022"
  dark-hover: "#242628"
  dark-selected: "#2A2D30"
  dark-hairline: "#2C2F32"
  dark-text-primary: "#ECEDEE"
  dark-text-secondary: "#AEB3B7"
  dark-text-tertiary: "#8A9095"
  dark-action: "#ECEEF0"
  dark-action-hover: "#FFFFFF"
  light-shell: "#F2F3F4"
  light-raised: "#FAFBFB"
  light-hover: "#ECEEEF"
  light-selected: "#E2E5E8"
  light-hairline: "#D9DDE0"
  light-text-primary: "#1C1E20"
  light-text-secondary: "#5D6367"
  light-text-tertiary: "#687076"
  light-action: "#1C1E20"
  light-action-hover: "#34383C"
  action-on: "#FFFFFF"
typography:
  body:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "13px"
    fontWeight: 400
    lineHeight: 1.5
  content:
    fontFamily: "'SF Mono', 'Cascadia Mono', ui-monospace, Menlo, monospace"
    fontSize: "13px"
    fontWeight: 400
    lineHeight: 1.5
  label:
    fontFamily: "-apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif"
    fontSize: "12px"
    fontWeight: 500
    lineHeight: 1.4
rounded:
  keycap: "4px"
  control: "6px"
  row: "8px"
  panel: "14px"
elevation:
  panel-dark: "0 2px 5px rgba(0,0,0,.18), 0 12px 24px rgba(0,0,0,.30)"
  panel-light: "0 2px 5px rgba(24,28,32,.10), 0 12px 24px rgba(24,28,32,.20)"
  menu: "0 6px 8px rgba(0,0,0,.25)"
spacing:
  xs: "4px"
  sm: "8px"
  md: "12px"
  lg: "16px"
  xl: "24px"
components:
  copy-button-dark:
    backgroundColor: "{colors.dark-action}"
    textColor: "{colors.light-text-primary}"
    rounded: "{rounded.control}"
    padding: "7px 15px"
  copy-button-light:
    backgroundColor: "{colors.light-action}"
    textColor: "{colors.action-on}"
    rounded: "{rounded.control}"
    padding: "7px 15px"
  list-row:
    rounded: "{rounded.row}"
    padding: "8px 8px"
    height: "44px"
---

# ClipClop 设计系统

## 1. Overview（概览）

**设计北极星：“安静的剪贴板取景器”**

ClipClop 是随叫随到的桌面工具，不是内容管理系统。外壳使用无彩石墨灰并主动退后，让用户复制的文本、图片、颜色、文件和链接成为画面主体。界面密度偏高、层级明确、键盘优先，不使用装饰性配色建立品牌感。

主界面为约 `720 × 540px` 的浮动面板：左侧约 `300px`，包含搜索和每页 10 条的列表；右侧为可滚动预览和固定元数据栏；底部为 `48px` 状态栏。面板跟随系统切换 dark/light，布局、间距和交互保持一致。

- 快速识别优先于展示更多元数据。
- 列表负责定位，预览负责确认，Enter 负责复制并关闭。
- v1 不加入类型筛选、标签、侧边栏、AI 信息增强或直接粘贴。
- 系统字体负责界面，等宽字体负责剪贴板内容；中文自动回退到系统中文字体。

## 2. Colors（颜色）

配色策略是严格无彩色。产品 UI 自身只能使用灰阶；图片、色值色块、文件图标、应用图标和已缓存 favicon 属于用户内容，可以保留原色。

### Dark 模式

| Token | 色值 | 用途 |
|---|---:|---|
| `--bg-shell` | `#17181A` | 面板底色 |
| `--bg-raised` | `#1E2022` | 预览与抬升表面 |
| `--bg-hover` | `#242628` | 行悬停 |
| `--bg-selected` | `#2A2D30` | 选中行 |
| `--border-hairline` | `#2C2F32` | 分隔线 |
| `--text-primary` | `#ECEDEE` | 正文与主要内容 |
| `--text-secondary` | `#AEB3B7` | 控件、来源应用、可操作提示；小字号保持清晰 |
| `--text-tertiary` | `#8A9095` | 非关键时间与元数据；不得用于需要快速识别的正文 |
| `--action` | `#ECEEF0` | 主复制按钮；与深色文字形成高对比 |
| `--action-hover` | `#FFFFFF` | 主按钮悬停 |
| `--action-on` | `#17181A` | 主按钮文字与按键图标 |

### Light 模式

Light 不是暗色反相：外壳使用浅灰而非纯白，预览表面略亮，选中态必须比 hover 深一个台阶，主要按钮使用深灰保证识别和对比度。

| Token | 色值 | 用途 |
|---|---:|---|
| `--bg-shell` | `#F2F3F4` | 面板底色，减少纯白眩光 |
| `--bg-raised` | `#FAFBFB` | 预览与抬升表面 |
| `--bg-hover` | `#ECEEEF` | 行悬停 |
| `--bg-selected` | `#E2E5E8` | 选中行 |
| `--border-hairline` | `#D9DDE0` | 分隔线 |
| `--text-primary` | `#1C1E20` | 正文与主要内容 |
| `--text-secondary` | `#5D6367` | 控件、来源应用、可操作提示 |
| `--text-tertiary` | `#687076` | 非关键时间与元数据，满足浅色背景正文对比度要求 |
| `--action` | `#1C1E20` | 主复制按钮；与白色文字形成高对比 |
| `--action-hover` | `#34383C` | 主按钮悬停 |
| `--action-on` | `#FFFFFF` | 主按钮文字 |

**纯灰规则。** v1 不使用品牌强调色、暖灰、冷灰染色、渐变或彩色选中条。状态不能只依赖颜色表达；焦点同时使用轮廓，错误同时提供文字。

## 3. Typography（字体）

界面字体使用 `-apple-system, BlinkMacSystemFont, "Segoe UI", system-ui, sans-serif`；剪贴板片段和预览使用 `"SF Mono", "Cascadia Mono", ui-monospace, Menlo, monospace`。这种组合让操作界面保持原生，让内容呈现像精确的工作台。

- 搜索：`13px/1.4`，正文权重 400。
- 列表内容：`13px/1.5`，等宽；最多两行，默认单行截断。
- 行序号与预览元数据：`11px/1.4`；关键操作信息用 secondary，字段名与时间用 tertiary。
- 状态栏与按钮：`12px/1.4`，权重 500–600。
- 长文本预览：`13px/1.6`，保留换行并允许断词；单行阅读宽度尽量不超过 75 个字符。
- 不使用全大写眉题、展示字体或过度字距；界面中不需要营销式标题层级。

## 4. Elevation（层级）

界面以色阶和 `1px` 分隔线建立内部层级。列表行、预览区和普通控件不使用阴影；只有浮动 Quick Panel 相对桌面获得一层环境阴影。系统透明材质仅可作为外壳增强，关闭透明效果或 Windows 不支持时必须回退到实色 `--bg-shell`。

- Quick Panel 的原生窗口比内容面板四周大 `20px`，保持透明且不绘制外框。阴影必须在这个透明安全边距中完整展开，不能被窗口裁掉。
- Quick Panel 使用连续的两层阴影：`0 2px 5px` 的低不透明度接触阴影稳定边缘，`0 12px 24px` 的环境阴影自然向桌面过渡。Dark 使用黑色 `.18/.30` 不透明度；Light 使用石墨色 `.10/.20`。这不是边框替代品，阴影之外不得再增加轮廓线。
- 操作菜单是唯一的内部浮层，使用 `0 6px 8px` 的短阴影和现有 hairline，避免与顶层面板竞争。
- 分隔：统一 `1px solid var(--border-hairline)`。
- 焦点：按钮、输入框与菜单使用 `2px solid var(--text-secondary)` 轮廓；列表是例外，容器不绘制外框，由当前 `aria-selected` 行的背景承担安静的键盘位置反馈。
- 面板出现：暗色/亮色均采用 `120ms` 淡入并从 `0.98` 缩放到 `1`；选择移动不做动画。删除已确认的记录时，消失行以 `90ms` 淡出，其余左侧列表行用 `180ms cubic-out` FLIP 位移平滑上移；不阻塞下一次输入。遵循 `prefers-reduced-motion`，此时直接更新。

## 5. Components（组件）

### Quick Panel

- 内容面板不允许用户缩放，圆角 `14px`；但不是单一固定尺寸。打开时依据当前显示器的**逻辑工作区**选择档位：小于 `1100 × 720px` 时为 `720 × 540px`，常规桌面为 `800 × 600px`，达到 `1600 × 900px` 时为 `960 × 720px`。尺寸始终不超过工作区减去阴影安全边距后的空间。
- 原生透明窗口在内容区四周额外保留 `20px` 阴影边距，因此三个外部窗口尺寸分别为 `760 × 580px`、`840 × 640px`、`1000 × 760px`。Web 面板不绘制外围 border，避免矩形窗口包住圆角内容。
- 顶部为 `42px` 品牌 chrome：左侧显示应用图标与 `ClipClop`，图标是应用菜单入口，菜单包含“设置…”与“退出 ClipClop”；图标之外的标题栏是无控件拖拽面。选择“设置…”后在**同一个面板**内切换到设置视图，不打开第二个系统窗口；历史列表与选中记录保留，返回后继续原位置。设置视图顶部提供返回，Esc 先返回历史。搜索位于左栏、列表滚动区之前。内容区为两列 `300px 1fr`，常规与大屏档左栏扩至 `320px`，底栏 `48px`。竖向分隔线只从内容区延续至状态栏，不穿过标题栏。
- Dark 为重点展示模式，Light 必须具备同等信息层级和 WCAG AA 可读性。

### 搜索

- 位于左栏顶部，高约 `40px`，左右内边距 `14px`，底部使用 hairline。
- 默认不抢焦点；按 `/` 或点击进入，Esc 清空焦点并返回列表。
- placeholder 使用 secondary 色以满足可读性；右侧 `/` 使用统一 keycap 样式。

### 列表与行

- 每页固定 10 条，最新在前；主行高约 `44px`、圆角 `8px`、间距 `1px`。只有一次复制包含两个及以上文件的 `file` 记录才是可展开的文件组：选中后再次点击，或按 `Space`，可展开／收起紧贴在主行下方的缩进文件名列表。其他文本、图片与单文件记录没有折叠箭头，也不占用展开语义；`←/→` 始终用于左侧翻页。折叠符号为右向 chevron，以 `160ms` 缓出旋转到下向状态，不增加卡片、阴影或外围边框。
- 结构为：`序号 + 28px 固定内容槽 + 片段`；列表不显示时间，来源应用只在预览元数据中显示。
- text/code/link 的内容槽留空；颜色显示真实色块；图片加载 `56px` 上限的实际缩略图，摘要显示像素尺寸而非“图片”；文件可预览时显示缩略图，不可预览时显示无文字的中性文件图标，摘要使用文件名。不得显示 `IMG`、`FILE`、`RTF` 等类型文字。
- hover 使用 `--bg-hover`；selected 只使用 `--bg-selected`，不加彩色边条、勾选图标或阴影。

### 预览

- 右侧上半部是纯预览画布：不滚动，内容始终在可用空间内 `contain`；图片／文件缩略图等比容纳，文本也只能在容器内裁切显示。文件名、完整路径、来源应用、复制时间与 KV facts 都属于底部 metadata，不能放入预览画布。metadata 栏固定；普通记录高约 `64px`，文件记录可扩至 `78px` 以容纳文件名与路径，不用中点或竖线串联信息。
- 时间固定为 `MM-DD HH:mm`；若不是当前年份才显示 `YYYY-MM-DD HH:mm`，不使用“今天”、相对时间或本地化长日期。
- 普通文本和代码均以安全纯文本预览；v1 不读取、保存、判断或展示 HTML/RTF 等文本格式。
- 图片 facts 显示像素尺寸与剪贴板大小；文本 facts 显示字符数与剪贴板大小。Finder 文件复制优先保存文件引用（路径、大小）并从真实文件生成预览，即使剪贴板同时提供系统文件图标也不能使用该图标作为预览。文件主体显示文件名、完整路径与缩略图，facts 显示文件数量和捕获时读取的真实文件大小；不能把 URI 引用 payload 的大小伪装为文件大小。
- 来源应用是捕获瞬间的尽力归因，而非剪贴板协议提供的绝对事实。剪贴板变化回调必须先冻结来源，再读取或编码内容；顺序依次为应用明确写入的来源字段、已知生产者的“专用格式 + 运行进程”严格签名、平台剪贴板所有者／前台应用、两秒内最近活动应用。Windows 优先使用剪贴板所有者进程；macOS 对 Snipaste 等不取得焦点的工具使用可扩展签名表。文件路径只说明复制内容，不得据此一律假设来源是 Finder。无法归因时隐藏来源名称和图标，只保留时间，不显示“未知来源”，也不回填旧记录。
- 链接预览显示原始 URL 和域名。v1 不抓取网页 title、description、Open Graph 等 SEO 信息，也不因剪贴板 URL 主动联网。

### 文本捕获

ClipClop 只把文本视为纯文本，不设任何文本格式类型，也不依据剪贴板附带的 HTML/RTF flavor 改变文本分类。搜索、列表摘要、预览、去重与复制全部基于纯文本；链接、代码和色值仅是对纯文本内容的轻量识别。

- v1 不读取、保存或回写 HTML/RTF；不转换或补造格式。
- concealed/transient 等平台标记不影响文本捕获；用户通过退出应用、忽略来源或删除内容控制本地记录。v1 不提供“暂停记录”，避免一个缺乏明确状态与一键入口的伪隐私开关。

### 文件缩略图

- 文件主体仍是引用：只保存路径和元数据，不复制源文件到应用目录。
- 一次复制多个 Finder 文件时，它们仍是一条剪贴板记录。右侧预览在元数据栏上方显示底部文件导航：左右按钮与缩略图带在这条记录的文件子项间切换，预览、文件名、路径和“在默认应用打开”同步到当前子项；metadata 的“文件”为当前项序号（如 `2/3`），大小为当前文件的实际大小，不能显示组总大小冒充当前文件属性。复制动作始终恢复整组文件。单文件记录不显示该导航。
- ClipClop 可以读取源文件一次，仅用于调用系统 Quick Look/Thumbnail API 生成小尺寸本地缩略图；缩略图是缓存，不代表托管文件。
- 列表缩略图输出最长边 `56px` 的 PNG；右侧预览最长边 `960 × 640px`。缩略图与右侧预览走不同资源路径，不能将原始图片传给列表；源文件变化或删除后允许显示旧缩略图并标记引用失效。
- 无法读取、不支持预览或生成超时时，回退为系统文件类型图标；不能阻塞剪贴板捕获主流程。

### 媒体文件

- 视频和音频仍是 `file` 记录，不增加“视频剪贴板”内容类型。Quick Panel 的键盘模型只服务定位、复制和记录操作，不嵌入可抢占 Space、方向键与 Tab 的 WebView 播放器；视频/音频在面板中按普通文件预览处理。
- Space 是所有内容类型的统一“在默认应用查看”动作：文件打开原文件，图片按需写入受控 PNG 预览文件，链接交给默认浏览器，文本/代码/色值按需写入受控纯文本预览文件。此动作只响应已选择记录；文件在后端验证路径存在后才交给系统默认应用。系统播放器承担媒体播放、字幕、音量和其自身的键盘无障碍行为。
- v1 不自建播放控制条、不转码、不引入 FFmpeg。未来如需快速观看，应以独立且显式进入/退出的媒体查看器实现，而不在 Quick Panel 的主键盘流程中混入第二套控制规则。

### favicon 与网页元信息

favicon 是网站的小图标，通常显示在浏览器标签页，例如 GitHub 的章鱼猫图标。它与 SEO 信息不同：SEO/网页元信息通常指页面标题、描述、Open Graph 图片等，需要请求网页并解析内容。

- v1 不主动联网获取 favicon 或 SEO 信息，避免泄露用户复制过哪些网址，并保持离线优先。
- 若操作系统或本地浏览器缓存已能提供 favicon，可使用本地缓存；否则链接内容槽留空。
- 如未来增加联网增强，必须默认关闭、显式授权、限定请求数据和缓存周期；这不是 v1 范围。

### 状态栏与操作

- 左侧为 `[←]  x/N  [→]`；箭头是 keycap，页码是普通信息文本。
- 右侧为 `⌘K 操作` 和 `⏎ 复制`。主按钮使用灰阶实色填充、圆角 `6px`；Dark 模式采用浅色按钮配深色文字，Light 模式采用深色按钮配白色文字。文字与按键图标必须继承同一个前景色，不得使用弱化的 secondary 灰。
- 全局呼出只做显示/隐藏：macOS 为 `⌃⌘C`（Control+Command+C），Windows 为 `Ctrl+Alt+C`。不占用 `⌘/Ctrl+C`、`⌘/Ctrl+V`、`⌘/Ctrl+Space`、`Win+V` 或 `Alt+Space`；`Fn` 不作为跨平台快捷键。当前版本不提供录制或修改快捷键。
- 面板每次取得系统焦点时自动将焦点置于列表，因此全局呼出后不需要 Tab 即可操作。列表是一个 roving-focus listbox，Tab 只进入搜索、列表、底部操作和已打开菜单，不会逐项停留在每条历史上。列表聚焦时 `↑/↓` 选择，`Home/End` 首尾选择，`←/→` 或 `PageUp/PageDown` 翻页，`1–0` 跳转可见行，Enter 复制并关闭，`/` 或 `⌘/Ctrl+F` 进入搜索。多文件记录出现时，`⌘←/⌘→`（Windows `Ctrl←/Ctrl→`）在**列表焦点内**切换组内文件；不会在搜索或其他文本控件中拦截。Tab 可进入右侧文件导航；此处 `←/→` 切换组内文件，`Home/End` 跳至首／末文件。导航按钮显示平台快捷键；当前缩略图获得键盘焦点时只使用轻微的内嵌轮廓，不绘制额外外框。
- `⌘K/Ctrl+K` 打开**当前记录**操作菜单；Windows 在列表聚焦时还支持 `Shift+F10`。菜单只包含与当前记录相关的“在默认应用查看”（Space）、忽略来源、删除和清空，不放设置或应用级操作；查看操作会根据内容类型显示“查看图片”“打开链接”“查看文本”等准确文案。`⌘/Ctrl+,` 打开设置。设置与退出位于左上角应用图标菜单；设置在面板内部完成，保存或返回不会中断历史浏览。菜单打开后焦点进入首个可用项目，`↑/↓`、`Home/End` 移动，Enter/Space 执行；Esc 先关菜单并返回触发按钮，再次 Esc 隐藏面板。点击窗口外也隐藏面板。
- Quick Panel 失去焦点（点击窗口外）时自动隐藏；再次按全局快捷键在显示与隐藏之间切换。应用继续在托盘运行，只有托盘“退出”才结束进程。
- macOS 与 Windows 均通过顶部右侧、与搜索栏同高的无控件 chrome 区拖动窗口；该区域不额外显示标题或手势光标。内容区（包括空白）不承担窗口拖动，确保搜索、选择、滚动和预览的交互语义稳定；Esc 在任何内部焦点状态下直接隐藏面板。
- 删除只删除 ClipClop 本地数据库中的记录及其格式 payload，不删除源文件、不修改系统当前剪贴板。入口位于操作菜单的“从 ClipClop 删除”，并在列表聚焦时支持 macOS `⌘⌫`、Windows `Ctrl+⌫`（亦接受物理 Delete 键）；执行前在底栏原位展示确认，不使用会导致面板失焦的浏览器确认框。确认态默认聚焦“删除/清空”，因此快捷键后直接 Enter 即执行；Esc 取消。删除后优先选中原行的下一条，若删除的是可见列表最后一条则选中上一条；不会跳回列表顶部。清空历史同样只清除本地数据库。操作菜单不做可搜索命令面板。

### 键盘与无障碍

- **快捷键分层。** 全局只保留呼出/隐藏；面板级使用 `⌘/Ctrl+,` 设置、`⌘/Ctrl+K` 当前记录操作、`⌘/Ctrl+F` 搜索；列表级使用方向键、数字键、Enter 与 Space（在默认应用查看当前记录）。任何快捷键都不重定义系统复制、粘贴或窗口管理键。
- **设置视图。** 进入设置后焦点直接落在第一个可编辑控件。Tab/Shift+Tab 按视觉与 DOM 顺序浏览原生复选框、下拉框、忽略应用的“移除”、返回和保存；Space 切换复选框，Enter/Space 执行按钮，选择框使用系统原生方向键行为；`⌘/Ctrl+S` 保存，Esc 返回历史。设置不采用自定义箭头键焦点模型，避免与标准表单控件冲突。
- **焦点与语义。** 列表使用 `listbox/option` 和 `aria-activedescendant`，由选中行色阶表达当前位置，避免额外粗框；普通按钮、输入框、菜单和设置控件保留可见的 `:focus-visible` 轮廓。菜单使用 `menu/menuitem`、`aria-expanded` 与键盘导航；异步保存结果通过 `aria-live="polite"` 宣布。
- **感知与容错。** 所有文字及可操作控件达到 WCAG AA 对比度；图标按钮有可读名称；状态不单靠颜色表达。动画只用于删除后重排，并在 `prefers-reduced-motion` 下关闭。焦点进入和返回都可预测，不通过自动滚动、焦点陷阱或隐藏的 Tab 停靠点阻碍键盘使用。

## 6. Do's and Don'ts（应做与禁用）

### 应做

- **应**让内容成为唯一稳定的彩色来源，并保持所有产品控件为灰阶。
- **应**在 Dark 与 Light 中维持相同布局和状态层级，分别校验文本与背景对比度。
- **应**为所有交互状态提供 default、hover、focus、active、disabled；加载使用局部骨架，不遮挡整个面板。
- **应**使用系统图标和标准键盘行为；文件缩略图失败时安静回退到文件类型图标。
- **应**让空历史只显示一句说明和呼出快捷键，搜索无结果只显示“无匹配结果”。

### 禁用

- **禁止**加入彩色强调色、渐变、玻璃卡片、发光效果、彩色类型图标或彩色选中条。
- **禁止**把 ClipClop 做成 AI 剪贴板、知识库、笔记系统、重型效率仪表盘或通用命令启动器。
- **禁止**加入永久应用导航、类型侧边栏、密集筛选 chips、过大的操作栏或元数据表格。
- **禁止**为每条记录增加独立卡片、宽阴影或超过 `16px` 的内容容器圆角。
- **禁止**自动渲染不受信任的 HTML、自动请求被复制的 URL、生成摘要、解释、标题或分类。
- **禁止**使用自定义滚动条、非标准弹窗和不表达状态的装饰动画。
