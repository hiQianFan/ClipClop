# ClipClop 隐私说明

简体中文 | [English](PRIVACY.md)

ClipClop 将剪贴历史保存在当前设备。应用没有账号、云同步、遥测、广告或内容分析服务，复制的 URL 也不会触发后台内容请求。

## 本地保存的数据

ClipClop 可能保存受支持的剪贴板内容、来源应用信息、文件路径引用和设置。应用不会自动识别或过滤敏感内容。文件记录只引用原始路径；用户明确选择或预览文件时，应用可能读取源文件以取得大小或生成缩略图，但不会复制、移动或上传它。

本地数据库依赖当前系统账户以及 FileVault、BitLocker 等磁盘加密保护。ClipClop 当前不提供应用层数据库加密。

## 网络访问

启用自动检查更新后，ClipClop 最多每 24 小时访问一次 GitHub Releases，用于比较版本和取得更新信息。请求不包含剪贴板内容、历史、文件路径或来源应用信息；自动检查可在设置中关闭。

## 你的控制方式

你可以删除单条记录、清空历史、设置保留期限，或退出 ClipClop 来停止捕获。卸载行为取决于操作系统和安装方式；如需移除全部本地数据，请先退出 ClipClop，再删除 `com.clipclop.desktop` 对应的应用数据目录。

如果未来加入遥测、云同步，或任何会处理剪贴板数据的新联网功能，本说明必须在发布前更新。
