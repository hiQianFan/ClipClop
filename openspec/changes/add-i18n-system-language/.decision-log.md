# Decision log

## 2026-07-20 — Initial proposal

- Chose `system | zh-CN | en` as preferences and `zh-CN | en` as effective locales.
- Kept `system` as the default preference and English as the universal non-Chinese fallback.
- Interpreted “Chinese system” as a primary locale whose base language subtag is `zh`; all `zh-*` variants temporarily use the Simplified Chinese catalog.
- Chose a first-party typed static catalog because the current two-locale desktop scope does not require remote catalogs or translation-platform integration.
- Accepted a development schema reset and strict new settings payload instead of permanent legacy compatibility.
- Kept release-note bodies and user/system-provided data outside translation ownership.

Self-validation passed: all five capabilities contain testable intent and success criteria; constraints alter implementation choices; non-goals bound the first release; and the proposal, design, delta specification, and task list preserve every load-bearing language, fallback, destructiveness, ownership, and verification decision from the source conversation. No unresolved questions remain for approval.
