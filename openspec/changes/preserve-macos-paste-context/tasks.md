## 1. 可行性验证（确认提案后执行）

- [ ] 1.1 固定 macOS/Chrome/WPS 复现环境，记录切换其他应用与现有 ClipClop 的 Esc/Enter 基线。
- [ ] 1.2 核对实际依赖调用链，实验去掉两处应用激活并保留原生面板/WKWebView 键盘输入。
- [ ] 1.3 验证中文输入法及 WPS 关闭面板后直接输入；记录通过/失败。失败则修订方案，不继续宣称 WPS 修复。

## 2. macOS 原生实现

- [ ] 2.1 统一 main/quick 非激活显示，覆盖快捷键、托盘、面板互切和首次启动；按实际 key 状态更新生命周期。
- [ ] 2.2 确保隐藏及 responder 交还的主线程完成顺序，不在主线程阻塞等待。
- [ ] 2.3 修正 blur/Quick Look 对应用激活状态的依赖，复用现有防过期 token。
- [ ] 2.4 目标仍活动时跳过激活；实现有界等待、受控回退、最终目标/权限检查和会话失效取消。
- [ ] 2.5 添加最小诊断：阶段、PID、面板 key/visible、耗时和 outcome；不记录正文、标题或 URL。

## 3. 前端兼容

- [ ] 3.1 验证现有 shown/focus/blur 事件及列表/搜索 DOM 焦点，优先不改前端。
- [ ] 3.2 如发现隐藏后或旧会话异步聚焦，局部修正 HistoryWorkspace/QuickPanel；保留 IPC 和现有键盘模式。

## 4. 验证及交付

- [ ] 4.1 在现有测试体系覆盖目标未变/已变、超时、旧会话、必要的前端焦点适配。
- [ ] 4.2 运行相关 Rust 测试/格式检查及 macOS、Windows Quality 检查；如前端变更，运行 pnpm check 和相关 Vitest。
- [ ] 4.3 执行 design.md 原生回归矩阵；WPS 核心 Esc 和 Enter 流程各连续 10 次通过，检查选区和单元格无误修改。
- [ ] 4.4 记录结果、限制和回退方式；未具备真实 WPS 环境时明确标为未验证，不能以单测通过代替。
