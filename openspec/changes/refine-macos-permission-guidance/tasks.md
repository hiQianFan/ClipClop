## 1. 确认提案

- [x] 1.1 用户确认本提案的主按钮切换、刷新图标和引导窗口方案后开始实现

## 2. 权限操作与刷新

- [x] 2.1 移除常驻当前 App 与旧条目说明，复用两个入口的状态逻辑
- [x] 2.2 移除倒计时重启，配置返回后提供不依赖 ready 的“重启应用”入口
- [x] 2.3 刷新改图标、补 tooltip/aria-label，合并查询并消除状态按钮闪烁

## 3. 引导窗口

- [x] 3.1 实现按需显示的非模态引导窗口，复用项目按钮与视觉 token
- [x] 3.2 实现真实当前 App 文件拖拽和键盘替代路径，处理开发进程与打开设置失败
- [x] 3.3 完全磁盘访问复用引导，不伪造已授权状态

## 4. 重启与恢复

- [x] 4.1 复用 relaunch，处理重复点击、失败、未保存设置和返回位置
- [x] 4.2 重启后重新查询，清除会话重启提示，确保不会循环提示重启

## 5. 验证

- [x] 5.1 运行相关行为测试和前端/Rust检查，覆盖缓存旧状态、返回但未授权、隐藏与并发查询
- [x] 5.2 在签名安装版实测拖拽、授权、用户重启和流程恢复
- [ ] 5.3 验证中文/英文、键盘操作、深浅色、Windows 无新增权限入口
- [x] 5.4 验证 OpenSpec 并提供实现后的检查清单

## 实施记录

- 分支：`feat/macos-permission-guidance`。既有签名改动及提案先保存为 `8456be0`。
- 拖拽使用 AppKit 原生文件拖拽会话；引导窗口内容区展示真实 `.app`，无障碍与完全磁盘访问都由用户主动拖入。无需新增拖拽依赖。
- 设置和 onboarding 共用 PermissionControls；用户点击后重启，保存或放弃未保存设置，恢复入口后重新检测。
- 签名安装版的拖拽、授权和重启恢复已验收；英文、键盘、深浅色与 Windows 回归仍保留在 5.3。
