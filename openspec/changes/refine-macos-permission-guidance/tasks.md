## 1. 确认提案

- [ ] 1.1 用户确认本提案的主按钮切换、刷新图标和引导窗口方案后开始实现

## 2. 权限操作与刷新

- [ ] 2.1 移除常驻当前 App 与旧条目说明，复用两个入口的状态逻辑
- [ ] 2.2 移除倒计时重启，配置返回后提供不依赖 ready 的“重启应用”入口
- [ ] 2.3 刷新改图标、补 tooltip/aria-label，合并查询并消除状态按钮闪烁

## 3. 引导窗口

- [ ] 3.1 实现按需显示的非模态引导窗口，复用项目按钮与视觉 token
- [ ] 3.2 实现真实当前 App 文件拖拽和键盘替代路径，处理开发进程与打开设置失败
- [ ] 3.3 完全磁盘访问复用引导，不伪造已授权状态

## 4. 重启与恢复

- [ ] 4.1 复用 relaunch，处理重复点击、失败、未保存设置和返回位置
- [ ] 4.2 重启后重新查询，清除会话重启提示，确保不会循环提示重启

## 5. 验证

- [ ] 5.1 运行相关行为测试和前端/Rust检查，覆盖缓存旧状态、返回但未授权、隐藏与并发查询
- [ ] 5.2 在签名安装版实测拖拽、授权、用户重启和流程恢复
- [ ] 5.3 验证中文/英文、键盘操作、深浅色、Windows 无新增权限入口
- [ ] 5.4 验证 OpenSpec 并提供实现后的检查清单
