# Decision log

- 2026-08-30: Chose feature-first composition rather than full frontend DDD. Existing feature/API/session boundaries are healthy; the proven problem is view and CSS ownership.
- 2026-08-30: Corrected the source report's A2 premise. `.ghost`, `.destructive`, `.menu`, and `.menu-separator` are currently consumed only inside `HistoryWorkspace`, so no shared CSS layer is a prerequisite.
- 2026-08-30: Rejected line-count, token-reference, hard-coded-pixel, and `:global()` count targets as architecture acceptance criteria. Behavior, ownership, dependency direction, build validity, and testability are authoritative.
- 2026-08-30: Coherence and preservation validation passed: four capabilities include testable success criteria; implementation detail is isolated in companions; all load-bearing corrected recommendations from the review are preserved. No blocking open question remains.
- 2026-08-30: Added CAP-5 as the sole intentional interaction change: static desktop UI is non-selectable with a default pointer; text inputs/editors and the right-side text/link body preview are explicit selection islands. This belongs in the global base and interaction contract, not repeated component CSS.
- 2026-08-30: Re-validation passed with five stable capabilities. The selection whitelist, excluded surfaces, implementation owner, manual platform verification, and behavior-freeze exception are all preserved with no open question.
