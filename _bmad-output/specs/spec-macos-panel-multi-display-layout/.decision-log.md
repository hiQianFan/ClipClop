# 决策记录

## 2026-08-29 · 初始规范

- 保留现有 Tauri WebView + NSPanel，只替换 macOS 面板的原生几何定位。
- Quick 与主面板使用同一原则但分阶段实现：全程 AppKit points、同一目标 screen、一次完整 frame 写入。
- 主面板保留尺寸算法而非现有异步投递方式；尺寸和 origin 合并落地，避免旧尺寸居中后再异步缩放。
- 第一阶段只实现 Quick，主面板单独提交和回滚。

自检结论：五项 SPEC 字段完整，3 项能力均含可验证成功条件；Quick、主面板、失败可观测性、历史失败机制、平台边界和实施顺序均已保留。实现细节与真机矩阵放入 `implementation.md`。无开放问题。
